<!-- prettier-ignore -->
<div class="{{ $classes }}" style="{{ $style }}" data-cm-sidebar-collapsed="{{ $sidebarCollapsed }}" data-cm-controller="app-shell shell-metrics"{!! $attributes !!}>@if ($header !== null)<header class="cm-app-shell__header" data-cm-sticky-header>{!! $header !!}</header>@endif@if ($subheader !== null)<div class="cm-app-shell__subheader" data-cm-sticky-subheader>{!! $subheader !!}</div>@endif<div class="cm-app-shell__body">@if ($sidebar !== null)<aside class="cm-app-shell__sidebar">{!! $sidebar !!}</aside>@endif<main class="cm-app-shell__content">{!! $content !!}</main>@if ($aside !== null)<aside class="cm-app-shell__aside">{!! $aside !!}</aside>@endif</div>@if ($footer !== null)<footer class="cm-app-shell__footer">{!! $footer !!}</footer>@endif</div>
