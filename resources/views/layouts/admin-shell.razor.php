<!-- prettier-ignore -->
<div class="{{ $classes }}"{!! $attributes !!}>@if ($hasTopbar)<header class="cm-admin-shell__topbar">@if ($brand !== null)<div class="cm-admin-shell__brand">{{ $brand }}</div>@endif@if ($header !== null)<div class="cm-admin-shell__header">{{ $header }}</div>@endif@if ($headerActions !== null)<div class="cm-admin-shell__header-actions">{{ $headerActions }}</div>@endif</header>@endif<div class="cm-admin-shell__body">@if ($sidebar !== null)<aside class="cm-admin-shell__sidebar"><div class="cm-admin-shell__sidebar-content">{{ $sidebar }}</div></aside>@endif<div class="cm-admin-shell__workspace"><main class="cm-admin-shell__content">{{ $content }}</main>@if ($footer !== null)<footer class="cm-admin-shell__footer">{{ $footer }}</footer>@endif</div></div></div>
