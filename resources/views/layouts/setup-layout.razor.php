<!-- prettier-ignore -->
<main class="{{ $classes }}" data-cm-controller="setup-layout"{!! $attributes !!}><div class="cm-setup-layout__panel">@if ($brand !== null)<div class="cm-setup-layout__brand">{{ $brand }}</div>@endif@if ($toolbar !== null)<div class="cm-setup-layout__toolbar">{{ $toolbar }}</div>@endif@if ($aside !== null)<aside class="cm-setup-layout__aside">{{ $aside }}</aside>@endif@if ($hasHeader)<header class="cm-setup-layout__header"><div class="cm-setup-layout__heading">@if ($title !== null)<h1 class="cm-setup-layout__title">{{ $title }}</h1>@endif@if ($description !== null)<p class="cm-setup-layout__description">{{ $description }}</p>@endif</div></header>@endif<section class="cm-setup-layout__main"><div class="cm-setup-layout__body">{{ $content }}</div>@if ($actions !== null)<div class="cm-setup-layout__actions">{{ $actions }}</div>@endif</section></div>@if ($footer !== null)<footer class="cm-setup-layout__footer">{{ $footer }}</footer>@endif</main>
