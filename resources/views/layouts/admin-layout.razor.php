<!-- prettier-ignore -->
<div class="{{ $classes }}" data-cm-controller="admin-layout" data-cm-sidebar-collapsed="{{ $sidebarCollapsed }}" data-cm-mobile-sidebar-open="{{ $mobileSidebarOpen }}"{!! $attributes !!}>@if ($hasAside)<aside class="cm-admin-layout__aside" id="{{ $id }}">@if ($brand !== null)<div class="cm-admin-layout__brand">{{ $brand }}</div>@endif@if ($aside !== null)<div class="cm-admin-layout__aside-content">{{ $aside }}</div>@endif</aside><div class="cm-admin-layout__mobile-backdrop" aria-hidden="true" data-cm-mobile-sidebar-close></div>@endif<div class="cm-admin-layout__main">@if ($hasAside || $header !== null || $mobileBrand !== null)<header class="cm-admin-layout__header">@if ($hasAside)<div class="cm-admin-layout__mobile-toggle">@if ($mobileToggle !== null){{ $mobileToggle }}@else<button class="cm-admin-layout__mobile-toggle-button" type="button" data-cm-mobile-sidebar-toggle aria-label="{{ $toggleLabel }}" aria-expanded="{{ $mobileSidebarOpen }}" aria-controls="{{ $id }}"><span class="cm-admin-layout__mobile-toggle-icon" aria-hidden="true"></span></button>@endif</div>@endif@if ($mobileBrand !== null)<div class="cm-admin-layout__mobile-brand">{{ $mobileBrand }}</div>@endif@if ($header !== null)<div class="cm-admin-layout__header-content">{{ $header }}</div>@endif</header>@endif<main class="cm-admin-layout__content">{{ $content }}</main>@if ($footer !== null)<footer class="cm-admin-layout__footer">{{ $footer }}</footer>@endif</div></div>
